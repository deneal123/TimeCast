# Backend (FastAPI) — зависимости приложения.
# Канон — pyproject.toml (uv). Этот файл — для обычного pip.
# Библиотека timecast ставится из соседнего каталога (editable).
-e ../timecast

fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
aiofiles>=23
python-dotenv>=1.0
pyyaml>=6.0
rich>=13
pydantic>=2.10
bcrypt>=4.1
