# ── TimeCast frontend — пример переменных окружения ──
# Скопируйте в `.env` (Create React App читает переменные с префиксом REACT_APP_):
#   cp .env.example .env

# Базовый URL backend API (включая префикс /server)
REACT_APP_API_BASE_URL=http://localhost:8080/server
