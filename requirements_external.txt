# GPU-сборка torch теперь является зависимостью библиотеки timecast
# (см. ../timecast/pyproject.toml: [[tool.uv.index]] с CUDA-индексом).
# Через pip GPU-колесо torch можно поставить отдельно:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
-r requirements.txt
